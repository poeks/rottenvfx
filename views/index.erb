<% if @movies %>
  <% @movies.each do |movie| %>

    <h3>
      <a href="<%= movie.url %>"><%= movie.title %> (<%= movie.year %>)</a>
    </h3>

    <p><strong><%= movie.critics_rating %> <%= movie.critics_score %>%</strong></p>

    <p><%= movie.consensus %></p>

  <% end %>
<% end %>
